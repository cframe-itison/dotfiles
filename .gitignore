# Compiled Python files
*.pyc

# Build artifacts (remctl .mcpb bundle)
/remctl/dist/
/remctl/mcpb/build/

# Folder view configuration files
.DS_Store
Desktop.ini

# Thumbnail cache files
._*
Thumbs.db

# Files that might appear on external disks
.Spotlight-V100
.Trashes

# Claude config — track explicit config files only; ignore runtime/state
/.claude/*
!/.claude/settings.json
!/.claude/CLAUDE.md
!/.claude/commands/
!/.claude/agents/
!/.claude/skills/
!/.claude/keybindings.json
